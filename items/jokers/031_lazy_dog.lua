return {
    data = {
        object_type = "Joker",
        key     = 'lazy_dog',
        atlas   = 'placeholder',
        pos     = MLIB.coords(0,0),
        rarity  = 1,
        cost    = 5,
        config =  {
            extra = { pow = 0.04 },
        },
        loc_vars = function(self, info_queue, card)
        end,
        calculate = function(self, card, context)
            
        end,
    }
}
